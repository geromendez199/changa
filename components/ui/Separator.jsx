import { View } from 'react-native';
import { Colors } from '../../constants/theme';
export default function Separator() { return <View style={{ height: 1, backgroundColor: Colors.divider, marginVertical: 8 }} />; }
